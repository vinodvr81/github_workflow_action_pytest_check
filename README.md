# github_workflow_action_pytest_check
To correctly setup actions GITHUB workflow setup which is part CI(continous integration) setup 

pytest --cov-config=.coveragerc

pytest --cov=tests

pytest -m smoke

pytest --html=report.html
